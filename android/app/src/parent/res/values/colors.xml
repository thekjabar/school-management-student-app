<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The parent app's own pale violet, so the window the OS paints while the
         process starts is already this app's colour rather than another's. -->
    <color name="launch_background">#F2EEFD</color>
</resources>
