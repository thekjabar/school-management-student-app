name: student_app
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.11.5

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  # Kurdish and Arabic are right-to-left; these carry the direction, the date
  # symbols and the Material strings for every locale so the framework does not
  # have to be told twice.
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  http: ^1.2.2

  # Opens the two attribution links the map licence carries. https only,
  # so it needs no permission - just the <queries> entry the manifest
  # gained beside it, which Android 11 and up require before an app may
  # even ask whether a browser exists.
  url_launcher: ^6.3.1
  shared_preferences: ^2.3.3
  intl: ^0.20.2

  # Push, through OneSignal rather than FCM directly: a large share of handsets
  # in this market are Huawei with no Google Play Services, and an FCM-only
  # stack reaches perhaps three-quarters of families without telling anyone
  # which quarter is missing. The server side already addresses by external id.
  onesignal_flutter: ^5.3.0

  # The opening animation is a rendered clip rather than a drawn logo, so it
  # needs a real decoder; there is no image format that plays.
  video_player: ^2.9.2

  # Fingerprint or face, for the one thing in this app a parent SENDS to the
  # school on their child's behalf.
  local_auth: ^2.3.0

  # The camera, for the one photograph the platform will not open a shift
  # without: the crew member standing at the bus. `shift-start` refuses any
  # asset id that is not a real file uploaded by the caller, so a handset that
  # cannot take a picture cannot file a walk-around at all — and without the
  # walk-around `depart` refuses to let the bus go. That is the whole morning,
  # not one button.
  #
  # image_picker rather than `camera`: it hands the capture to the phone's own
  # camera app, so there is no preview surface to keep alive and no CameraX
  # lifecycle to get wrong across the forty kinds of handset this ships to.
  image_picker: ^1.1.2

  # A real map, with real tiles. Chosen over the Google Maps SDK because it
  # needs no API key and no billing account: every school that deploys this
  # would otherwise have to hold one, and OpenStreetMap's coverage of Erbil and
  # Sulaymaniyah is good.
  flutter_map: ^8.1.1
  latlong2: ^0.9.1
  geolocator: ^14.0.3
  mapbox_maps_flutter: ^2.30.0

  # Google Maps, for the parent tracking screen only.
  #
  # The native Maps SDK for Android and iOS bills nothing for what this screen
  # does: drawing the map, moving the camera, and placing OUR OWN markers and
  # polylines. The metered products are Places, Geocoding, Routes and Distance
  # Matrix, and not one of them is called from here — addresses still come from
  # Nominatim through lib/api/geocode.dart, and the line to the stop is drawn
  # from coordinates the platform already sends us.
  #
  # flutter_map stays: the driver's route map is built on it, and a driver's
  # handset is the one most likely to have no Play Services at all.

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # The bus scene on the parent home screen, taken from the design itself —
  # nothing assembled out of Material icons was going to be the same picture.
  assets:
    - assets/art/
    - assets/video/

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
